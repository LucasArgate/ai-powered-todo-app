// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id                String   @id @default(cuid())
  name              String?
  isAnonymous       Boolean  @default(true)
  aiIntegrationType AiIntegrationType?
  aiToken           String?
  aiModel           String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  // Relations
  taskLists TaskList[]

  @@map("users")
}

model TaskList {
  id          String   @id @default(cuid())
  userId      String
  name        String
  description String?
  iaPrompt    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relations
  user  User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  tasks Task[]

  @@map("task_lists")
}

model Task {
  id          String   @id @default(cuid())
  listId      String
  title       String
  description String?
  isCompleted Boolean  @default(false)
  position    Int
  priority    String   @default("medium") // low, medium, high
  category    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relations
  taskList TaskList @relation(fields: [listId], references: [id], onDelete: Cascade)

  @@map("tasks")
}

model Provider {
  id                String   @id @default(cuid())
  name              String   @unique
  description       String
  free              Boolean  @default(false)
  models            String   // JSON array of available models
  tokenUrl          String?  // URL to get API token
  documentationUrl  String?  // URL to provider documentation
  isActive          Boolean  @default(true)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@map("providers")
}

enum AiIntegrationType {
  huggingface
  openrouter
  gemini
}